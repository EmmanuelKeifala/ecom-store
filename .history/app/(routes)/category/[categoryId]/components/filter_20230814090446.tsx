'use client'

import { Color, Size } from "@/types";

interface FilterProps{
    data: (Size | Color)[]
    name: string
    valueKey: 
}
const Filter = () => {
    return (
        <div></div>
    )
}
export default Filter;